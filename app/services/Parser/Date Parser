import re

def parse_date(text):

    m = re.search(r"\d{4}[-/.]\d{1,2}[-/.]\d{1,2}", text)

    if m:
        return m.group(0)

    m = re.search(r"\d{1,2}[./-]\d{1,2}[./-]\d{4}", text)

    if m:
        return m.group(0)

    return None